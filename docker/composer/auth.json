{
  "bitbucket-oauth": {
    "bitbucket.org": {
      "consumer-key": "${GH_BITBUCKET_KEY}",
      "consumer-secret": "${GH_BITBUCKET_SECRET}"
    }
  },
  "http-basic": {
    "composer.garrisonhughes.site": {
      "username": "${GH_COMPOSER_USERNAME}",
      "password": "${GH_COMPOSER_PASSWORD}"
    }
  }
}
